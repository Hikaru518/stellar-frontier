## Docs Exploration

- 选择：先扫描现有 docs
- 目的：找出与本轮 IAFS 开局、坠毁地点、维修事件、行动入口、地图封锁、属性检定相关的现有约束，避免和当前文档体系冲突。

## Web Research

- 选择：不用探索

## Core Rule Decisions To Confirm In This Round

- 敏捷检定：选择“敏捷决定成功率”，默认公式为 `成功率 = 敏捷 * 20% - 难度`。
- 可配置项：不同事件可单独配置 `ratio` 与 `bias`；默认 `ratio = 20%`，默认难度/偏置为 `0`，因此敏捷 `5` 可保证 `100%` 成功。
- 并发维修：同一对象同一时间只允许一个队员维修；其他队员看到对象占用状态，不可同时维修。
